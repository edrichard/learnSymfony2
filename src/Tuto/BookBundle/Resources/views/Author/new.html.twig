{% extends 'TutoBookBundle::layout.html.twig' %}

{% block title %}Author creation{% endblock %}

{% block content %}

<form action="{{ path('author_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('author') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}